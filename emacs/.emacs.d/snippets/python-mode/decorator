# -*- mode: snippet -*-
# name: decorator function
# key: decorator
# --
def ${1:decorator_name}(${2:func}):
    """${3:Decorator description.}"""
    def wrapper(*args, **kwargs):
        ${4:# Pre-processing}
        result = ${2:$(yas-field-value 2)}(*args, **kwargs)
        ${5:# Post-processing}
        return result
    return wrapper
 